# ===========================================
# WooCommerce Attribution Bridge - Docker Environment
# ===========================================
# Copy this file to .env.docker and fill in your values
#
# Usage:
#   cp .env.docker.example .env.docker
#   docker compose --env-file .env.docker up -d
#
# ===========================================

# -----------------------------------------
# PostgreSQL (only needed with docker-compose.yml)
# -----------------------------------------
# These are used when running PostgreSQL in Docker
POSTGRES_USER=wab
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=wab_dashboard
POSTGRES_PORT=5432

# -----------------------------------------
# Database URL (only needed with docker-compose.external-db.yml)
# -----------------------------------------
# For external databases (Supabase, Neon, RDS, etc.)
# Uncomment and set when using docker-compose.external-db.yml
# DATABASE_URL=postgresql://user:password@host:5432/database

# -----------------------------------------
# NextAuth Configuration (REQUIRED)
# -----------------------------------------
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-32-character-secret-here

# Your dashboard URL (no trailing slash)
# Use your actual domain in production
NEXTAUTH_URL=http://localhost:3000

# -----------------------------------------
# Google OAuth (Optional)
# -----------------------------------------
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: {NEXTAUTH_URL}/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# -----------------------------------------
# Dashboard Port (Optional)
# -----------------------------------------
# Change if port 3000 is already in use
DASHBOARD_PORT=3000
